import {buildBookSectionSchema} from './buildSchema';
import {BookSectionSchema} from './schema';

export const bookSectionSchema = {
  schema: BookSectionSchema,
  buildSchema: buildBookSectionSchema,
};
