import React from 'react';

import {Box, Text} from '@components';

type Props = {};

export default function ErrorScreen({}: Props) {
  return (
    <Box flex={1}>
      <Text text="Book Screen" color="black" />
    </Box>
  );
}
