import React from 'react';

import {SharedWrapperScreen} from '@shared';

import {Text} from '@components';

export function SearchScreen() {
  return (
    <SharedWrapperScreen>
      <Text text="SearchScreen" color="base" />
    </SharedWrapperScreen>
  );
}
