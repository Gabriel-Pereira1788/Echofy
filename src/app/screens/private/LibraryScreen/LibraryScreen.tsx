import React from 'react';

import {SharedWrapperScreen} from '@shared';

import {Text} from '@components';

export function LibraryScreen() {
  return (
    <SharedWrapperScreen>
      <Text text="LibraryScreen" color="base" />
    </SharedWrapperScreen>
  );
}
